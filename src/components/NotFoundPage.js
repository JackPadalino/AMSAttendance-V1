import React from 'react';

const NotFoundPage = () => {
    return (
        <div>
           <p>Looks like you aren't supposed to be here!</p> 
        </div>
    );
};

export default NotFoundPage;